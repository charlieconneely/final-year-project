import React from 'react';
import { Button } from '@material-ui/core'

function LandingPage() {

    return (
        <div>
            <Button color="secondary">Host Room</Button>
            <Button color="secondary">Join Room</Button>
        </div>
    )
}

export default LandingPage